import { useState } from "react";
import { useLocation } from "wouter";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Label } from "@/components/ui/label";
import { useToast } from "@/hooks/use-toast";
import { apiRequest } from "@/lib/queryClient";
export default function AdminLogin() { const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [loading, setLoading] = useState(false); const [, setLocation] = useLocation(); const { toast } = useToast(); const submit = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); try { await apiRequest("POST", "/api/auth/login", { username, password }); setLocation("/admin"); } catch { toast({ title: "Login failed", description: "Invalid username or password", variant: "destructive" }); } finally { setLoading(false); } }; return <main className="min-h-screen overflow-x-hidden bg-primary text-primary-foreground grid lg:grid-cols-2"><div className="hidden lg:flex flex-col justify-between p-12 border-r border-primary-foreground/15"><div><span className="inline-block bg-white px-4 py-2.5"><img src="/reggycodas-logo.png" alt="ReggyCodas — Where Solutions Count" className="h-12 w-auto" /></span></div><div><p className="eyebrow text-secondary">Private workspace</p><h1 className="display-type mt-5 text-7xl font-bold leading-none">Make<br />things<br /><span className="text-accent">grow.</span></h1></div><p className="text-sm text-primary-foreground/50">Nairobi · Kenya</p></div><div className="flex min-h-[100dvh] items-center justify-center px-5 py-16 sm:px-8"><div className="w-full max-w-md"><div className="mb-10 lg:hidden"><span className="inline-block rounded bg-white px-4 py-2.5"><img src="/reggycodas-logo.png" alt="ReggyCodas — Where Solutions Count" className="h-10 w-auto" /></span></div><p className="eyebrow text-secondary">Studio access</p><h2 className="display-type mt-4 text-4xl font-bold sm:text-5xl">Welcome back.</h2><p className="mt-4 text-primary-foreground/60">Sign in to manage what we’re building.</p><form onSubmit={submit} className="mt-10 space-y-6"><div><Label htmlFor="username" className="text-primary-foreground/70">Username</Label><Input id="username" value={username} onChange={e => setUsername(e.target.value)} className="mt-2 min-h-11 border-primary-foreground/20 bg-primary-foreground/10 text-primary-foreground placeholder:text-primary-foreground/40" placeholder="Your username" /></div><div><Label htmlFor="password" className="text-primary-foreground/70">Password</Label><Input id="password" type="password" value={password} onChange={e => setPassword(e.target.value)} className="mt-2 min-h-11 border-primary-foreground/20 bg-primary-foreground/10 text-primary-foreground placeholder:text-primary-foreground/40" placeholder="Your password" /></div><Button type="submit" disabled={loading} className="min-h-11 w-full bg-accent text-accent-foreground hover:bg-secondary hover:text-primary">{loading ? "Checking..." : "Enter workspace →"}</Button></form></div></div></main>; }