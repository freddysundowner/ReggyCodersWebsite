import { useQuery } from "@tanstack/react-query";
import { Link } from "wouter";
import { Twitter, Linkedin, Github, Instagram, Facebook, Youtube, ArrowUpRight } from "lucide-react";
import { SiTiktok } from "react-icons/si";
import type { SocialLink } from "@shared/schema";
const icons: Record<string, any> = { Twitter, LinkedIn: Linkedin, GitHub: Github, Instagram, Facebook, YouTube: Youtube, TikTok: SiTiktok };
export default function Footer() { const { data } = useQuery<SocialLink[]>({ queryKey: ["/api/social-links"] }); const scroll = (id: string) => document.getElementById(id)?.scrollIntoView({ behavior: "smooth" }); return <footer className="bg-primary py-16 text-primary-foreground"><div className="mx-auto max-w-7xl px-5 lg:px-10"><div className="flex flex-col justify-between gap-12 md:flex-row"><div><button onClick={() => scroll("home")} className="display-type text-2xl font-bold">reggycodas<span className="text-secondary">.</span></button><p className="mt-5 max-w-xs text-sm leading-6 text-primary-foreground/60">Product studio and startup workshop, based in Nairobi.</p><div className="mt-6 flex gap-3">{data?.filter(l => l.url).map(l => { const Icon = icons[l.platform]; return <a key={l.id} href={l.url!} target="_blank" rel="noreferrer" className="grid h-9 w-9 place-items-center border border-primary-foreground/20 hover:bg-secondary hover:text-primary transition-colors" aria-label={l.platform}>{Icon ? <Icon size={15} /> : l.platform[0]}</a> })}</div></div><div className="grid grid-cols-2 gap-x-20 gap-y-3 text-sm"><div className="space-y-3"><p className="eyebrow text-secondary">Explore</p><button onClick={() => scroll("about")} className="block text-primary-foreground/70 hover:text-secondary">About</button><button onClick={() => scroll("products")} className="block text-primary-foreground/70 hover:text-secondary">Products</button><Link href="/blog" className="block text-primary-foreground/70 hover:text-secondary">Journal</Link></div><div className="space-y-3"><p className="eyebrow text-secondary">Connect</p><button onClick={() => scroll("contact")} className="block text-primary-foreground/70 hover:text-secondary">Start a project</button><a href="mailto:info@reggycodas.com" className="flex items-center gap-1 text-primary-foreground/70 hover:text-secondary">Email us <ArrowUpRight size={13} /></a></div></div></div><div className="mt-16 border-t border-primary-foreground/15 pt-6 flex flex-col gap-2 text-xs text-primary-foreground/40 md:flex-row md:justify-between"><span>© 2024 Reggycodas Ltd.</span><span>Made with care in Kenya.</span></div></div></footer>; }