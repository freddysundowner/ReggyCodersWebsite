import { ArrowUpRight } from "lucide-react";
export default function StartupsSection() { const go = () => document.getElementById("contact")?.scrollIntoView({ behavior: "smooth" }); const steps = [["01", "Find the signal", "We sharpen the idea until the real opportunity is impossible to miss."], ["02", "Build the first proof", "A focused product, shipped quickly, with enough substance to learn from."], ["03", "Make it move", "We help with the systems, strategy and momentum to turn traction into a business."]]; return <section id="startups" className="bg-secondary py-24 lg:py-32"><div className="mx-auto max-w-7xl px-5 lg:px-10"><div className="flex flex-col justify-between gap-8 md:flex-row"><div><p className="eyebrow text-primary">03 / Incubation</p><h2 className="display-type mt-5 max-w-3xl text-5xl font-bold leading-none text-primary md:text-7xl">Bring us the<br />messy beginning<span className="text-accent">.</span></h2></div><p className="max-w-sm self-end text-sm leading-6 text-primary/70">For founders with a sharp problem and the appetite to stay with it. We bring product thinking, engineering and honest questions.</p></div><div className="mt-20 grid border-t border-primary/20 md:grid-cols-3">{steps.map(([num, title, text]) => <div key={num} className="border-b border-primary/20 py-8 md:border-b-0 md:border-r md:px-8 md:first:pl-0"><p className="mono-type text-xs text-accent">{num}</p><h3 className="display-type mt-10 text-3xl font-bold text-primary">{title}</h3><p className="mt-4 text-sm leading-6 text-primary/70">{text}</p></div>)}</div><button onClick={go} className="mt-14 flex items-center gap-3 bg-primary px-6 py-4 text-sm font-bold text-primary-foreground hover:bg-accent transition-colors">Tell us what you’re building <ArrowUpRight size={17} /></button></div></section>; }